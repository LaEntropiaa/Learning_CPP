#include <iostream>
#include <fstream>
#include <vector>
#include <string>

int main()
{
    std::ofstream file ("guess_game");
    std::string current_line = "try";
    std::vector<std::string> file_data;
   

    file.close();

    return 0;
}
